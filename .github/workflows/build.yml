name: Build Flutter APK

on:
  workflow_dispatch: # این خط اجازه می‌دهد بیلد را دستی با یک دکمه اجرا کنید
  push:
    branches:
      - main
      - master

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: ☕ Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17' # جاوا ۱۷ بهترین نسخه برای فلاتر جدید است

      - name: 🐦 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          # فلاتر نسخه پایدار را دانلود می‌کند

      - name: 📦 Install Dependencies
        run: flutter pub get

      - name: 🛠️ Build APK
        run: flutter build apk --release

      - name: 📤 Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Dowr-Game-APK
          path: build/app/outputs/flutter-apk/app-release.apk
